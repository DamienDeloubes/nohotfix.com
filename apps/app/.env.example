# ── apps/app (SPA dashboard) ─────────────────────────────────────────────────
# URL of apps/api — used for all authenticated API calls and token refresh
# Production: https://api.nohotfix.com
VITE_API_URL=http://localhost:3001

# URL of apps/web — used for post-logout redirect
# Production: https://nohotfix.com
VITE_WEB_URL=http://localhost:3000
